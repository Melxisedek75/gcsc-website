# AI Review Record

- Change ID: 2026-07-03-p1-3-persist
- Repository: gcsc-smart-contractor
- Branch: fix/p1-3-persist-receipts
- Base commit: d62cfd035cd4b6726ed451ea80c12dd45e6715e4
- Head commit: ed98ce7a74a00dfb5156aa8d6c866e2f9e5e7a56
- Author AI: CODEX
- Reviewer AI: CLAUDE
- Author status: READY_FOR_REVIEW
- Reviewer decision: NOT_REVIEWED
- Required checks: PARTIAL
- Unresolved P0/P1 findings: 1
- Live-risk decision: BLOCKED
- Founder evidence: PENDING
- Deploy decision: BLOCKED

## Scope

Persist 402 payment receipts, lead tokens, and job-posting payments in PostgreSQL so replay protection survives process restarts and replica scale-out. Enforce atomic writes and database uniqueness for normalized transaction hashes.

## Changed Files

- `v3/pure-server.js`
- `v3/package.json`
- `v3/tests/payments-402.test.js`
- `v3/tests/payment-receipts-postgres.test.js`

## Verification

| Check | Command | Result | Evidence |
|---|---|---|---|
| Static server check | `node --check v3/pure-server.js` | PASS | Exit 0 |
| Static test checks | `node --check v3/tests/payments-402.test.js` and `node --check v3/tests/payment-receipts-postgres.test.js` | PASS | Exit 0 |
| Focused regression suite | `npx jest tests/payments-402.test.js --runInBand --detectOpenHandles` from `v3` | PASS | 15/15 tests |
| Diff hygiene | `git diff --check` | PASS | No whitespace errors |
| PostgreSQL persistence/concurrency | `TEST_DATABASE_URL=... npm run test:payment-receipts-postgres` | NOT_RUN | No approved local PostgreSQL instance; live Railway database was not used |

## Findings

| Severity | Finding | Owner | Status |
|---|---|---|---|
| P1 | PostgreSQL integration suite must run against an isolated test database before approval/merge | CODEX / CLAUDE reviewer | OPEN |

## Resolution Log

- Added PostgreSQL tables `payment_receipts`, `lead_tokens`, and `job_posting_payments`, each with `UNIQUE(tx_hash)`.
- Added PostgreSQL transaction helper and atomic receipt/domain-row inserts.
- Mapped PostgreSQL unique violations (`23505`) to HTTP 409 replay responses.
- Persisted job publication state in the same transaction as its receipt.
- Normalized accepted hex transaction hashes to lowercase before lookup, verification, storage, and response.
- Added restart-style persistence, project publication, and concurrent replay integration tests for PostgreSQL.
- Pushed commit `ed98ce7` to `origin/fix/p1-3-persist-receipts`.

## Reviewer Notes (CLAUDE, 2026-07-03)

- Reviewer independently inspected the diff: YES (`git show ed98ce7`, full route wiring + schema)
- Reviewer independently ran required checks: PARTIAL (`node --check` PASS on server + both test files)
- Public/live/legal/payment boundary reviewed: YES

### Positives (persistence design is sound)
- Tables `payment_receipts` / `lead_tokens` / `job_posting_payments` each with `UNIQUE(tx_hash)` + unique indexes. Correct.
- `withPostgresTransaction` BEGIN/COMMIT/ROLLBACK helper; receipt + domain row inserted atomically. Correct.
- Postgres unique violation `23505` → 409 `payment_already_processed`. Correct replay protection that now survives restart.
- Keeps an in-memory fallback for the non-Postgres path. Reasonable.
- `node --check` PASS on all three touched files.

### 🔴 CRITICAL — CHANGES_REQUESTED: P1-3 regressed P1-1 (sender binding)
Although `ed98ce7` sits on top of `47e4c3f` (my P1-1/P1-2/P1-5), Codex rewrote both payment
routes and the `verifyHyperionTransfer` call wholesale from the pre-P1-1 (d62cfd0) shape.
Net effect at branch tip:
- `expectedFrom` count dropped from **5 (at 47e4c3f) → 0 (at ed98ce7)**.
- The lead-token and job-posting routes no longer resolve the caller wallet
  (`findUserById` → `wallet_required` 409) and no longer pass `expectedFrom` into
  `verifyHyperionTransfer`. The `bad_sender` rejection is gone.
- Result: **P1-1 is un-fixed** — any logged-in user of the right role can again claim
  another account's public tx hash. The exact vulnerability the audit flagged.

Required fix: rebase/merge so P1-3 preserves P1-1 — re-add the caller-wallet lookup +
`expectedFrom: <bound wallet>` on both `verifyHyperionTransfer` calls (require a connected
wallet, reject `bad_sender`), on top of the new persistence path.

### P1 (author already disclosed) — Postgres integration suite NOT_RUN
Needs a live/isolated Postgres (`TEST_DATABASE_URL`) run before merge. Blocks approval on its own.

## Sign-off

- Reviewer decision: CHANGES_REQUESTED
- Required checks: PARTIAL (node --check PASS; Postgres integration NOT_RUN)
- Unresolved P0/P1 findings: 2 (P1-1 regression reintroduced + Postgres suite not run)
- Live-risk decision: BLOCKED
- Founder evidence: PENDING
- Deploy decision: BLOCKED
