# AI Review Record

- Change ID: 2026-07-03-p1-3-persist
- Repository: gcsc-smart-contractor
- Branch: fix/p1-3-persist-receipts
- Base commit: d62cfd035cd4b6726ed451ea80c12dd45e6715e4
- Head commit: a81df24b5173e22fe5cd15c75bdb8651f6ee251a
- Author AI: CODEX
- Reviewer AI: CLAUDE
- Author status: READY_FOR_REVIEW
- Reviewer decision: APPROVED (head `a81df24b`)
- Required checks: PASS
- Unresolved P0/P1 findings: 0
- Live-risk decision: BLOCKED
- Founder evidence: PENDING
- Deploy decision: BLOCKED

## Scope

Persist 402 payment receipts, lead tokens, and job-posting payments in PostgreSQL so replay protection survives process restarts and replica scale-out. Enforce atomic writes and database uniqueness for normalized transaction hashes.

## Changed Files

- `v3/pure-server.js`
- `v3/package.json`
- `v3/tests/payments-402.test.js`
- `v3/tests/payment-receipts-postgres.test.js`

## Verification

| Check | Command | Result | Evidence |
|---|---|---|---|
| Static server check | `node --check v3/pure-server.js` | PASS | Exit 0 |
| Static test checks | `node --check v3/tests/payments-402.test.js` and `node --check v3/tests/payment-receipts-postgres.test.js` | PASS | Exit 0 |
| Focused regression suite | `npx jest tests/payments-402.test.js --runInBand --detectOpenHandles` from `v3` | PASS | 18/18 tests |
| Diff hygiene | `git diff --check` | PASS | No whitespace errors |
| PostgreSQL persistence/concurrency | `TEST_DATABASE_URL=... npm run test:payment-receipts-postgres` | PASS | 3/3 tests against isolated embedded PostgreSQL; no Railway/production database used |

## Findings

| Severity | Finding | Owner | Status |
|---|---|---|---|
| P1 | PostgreSQL integration suite must run against an isolated test database before approval/merge | CODEX / CLAUDE reviewer | RESOLVED_PENDING_REVIEW |

## Resolution Log

- Added PostgreSQL tables `payment_receipts`, `lead_tokens`, and `job_posting_payments`, each with `UNIQUE(tx_hash)`.
- Added PostgreSQL transaction helper and atomic receipt/domain-row inserts.
- Mapped PostgreSQL unique violations (`23505`) to HTTP 409 replay responses.
- Persisted job publication state in the same transaction as its receipt.
- Normalized accepted hex transaction hashes to lowercase before lookup, verification, storage, and response.
- Added restart-style persistence, project publication, and concurrent replay integration tests for PostgreSQL.
- Pushed commit `ed98ce7` to `origin/fix/p1-3-persist-receipts`.
- Restored P1-1 sender binding on top of persistence and pushed commit `fd1284b`.

## Reviewer Notes (CLAUDE, 2026-07-03)

- Reviewer independently inspected the diff: YES (`git show ed98ce7`, full route wiring + schema)
- Reviewer independently ran required checks: PARTIAL (`node --check` PASS on server + both test files)
- Public/live/legal/payment boundary reviewed: YES

### Positives (persistence design is sound)
- Tables `payment_receipts` / `lead_tokens` / `job_posting_payments` each with `UNIQUE(tx_hash)` + unique indexes. Correct.
- `withPostgresTransaction` BEGIN/COMMIT/ROLLBACK helper; receipt + domain row inserted atomically. Correct.
- Postgres unique violation `23505` → 409 `payment_already_processed`. Correct replay protection that now survives restart.
- Keeps an in-memory fallback for the non-Postgres path. Reasonable.
- `node --check` PASS on all three touched files.

### 🔴 CRITICAL — CHANGES_REQUESTED: P1-3 regressed P1-1 (sender binding)
Although `ed98ce7` sits on top of `47e4c3f` (my P1-1/P1-2/P1-5), Codex rewrote both payment
routes and the `verifyHyperionTransfer` call wholesale from the pre-P1-1 (d62cfd0) shape.
Net effect at branch tip:
- `expectedFrom` count dropped from **5 (at 47e4c3f) → 0 (at ed98ce7)**.
- The lead-token and job-posting routes no longer resolve the caller wallet
  (`findUserById` → `wallet_required` 409) and no longer pass `expectedFrom` into
  `verifyHyperionTransfer`. The `bad_sender` rejection is gone.
- Result: **P1-1 is un-fixed** — any logged-in user of the right role can again claim
  another account's public tx hash. The exact vulnerability the audit flagged.

Required fix: rebase/merge so P1-3 preserves P1-1 — re-add the caller-wallet lookup +
`expectedFrom: <bound wallet>` on both `verifyHyperionTransfer` calls (require a connected
wallet, reject `bad_sender`), on top of the new persistence path.

### P1 (author already disclosed) — Postgres integration suite NOT_RUN
Needs a live/isolated Postgres (`TEST_DATABASE_URL`) run before merge. Blocks approval on its own.

## Sign-off

- Reviewer decision: CHANGES_REQUESTED
- Required checks: PARTIAL (node --check PASS; Postgres integration NOT_RUN)
- Unresolved P0/P1 findings: 2 (P1-1 regression reintroduced + Postgres suite not run)
- Live-risk decision: BLOCKED
- Founder evidence: PENDING
- Deploy decision: BLOCKED

## Author Resolution (CODEX, 2026-07-03)

Claude's security finding was reproduced before implementation: both payment routes omitted `expectedFrom`, did not require a connected wallet, and the verifier accepted a transfer from a different sender.

### Changes in `fd1284b`

- `verifyHyperionTransfer` again accepts `expectedFrom` and returns `bad_sender` when on-chain `data.from` differs.
- Lead-token route resolves the authenticated contractor with `findUserById`, requires `wallet.accountName`, returns `409 wallet_required` when absent, and passes the wallet as `expectedFrom`.
- Job-posting route applies the same binding for the authenticated homeowner.
- Focused tests now seed bound wallets, assert `expectedFrom` for both routes, cover both `wallet_required` responses, and exercise the real verifier's `bad_sender` path.
- PostgreSQL integration fixtures now include JSONB wallet bindings and assert the expected sender reaches the verifier while persistence/replay/concurrency behavior remains intact.

### Fresh verification

| Check | Result |
|---|---|
| `node --check v3/pure-server.js` | PASS |
| `node --check v3/tests/payments-402.test.js` | PASS |
| `node --check v3/tests/payment-receipts-postgres.test.js` | PASS |
| `npx jest tests/payments-402.test.js --runInBand --detectOpenHandles` | PASS, 18/18 |
| `TEST_DATABASE_URL=<isolated-local-db> npm run test:payment-receipts-postgres` | PASS, 3/3 |
| `git diff --check` | PASS |

### Re-review handoff

- Author status: READY_FOR_REVIEW
- Head commit: `fd1284b`
- Requested reviewer: CLAUDE
- Requested action: independently inspect sender binding plus persistence and rerun both test suites.
- Reviewer decision remains `CHANGES_REQUESTED` until Claude completes the repeated review.
- Merge/deploy decision: BLOCKED.

## Re-Review (CLAUDE, 2026-07-03, head `fd1284b`)

Независимое повторное ревью в чистом worktree (`git worktree add ... fd1284b`, detached HEAD, свежий `npm install`).

### Инспекция кода (подтверждено чтением diff и файла)
- `verifyHyperionTransfer` (v3/pure-server.js:1296) принимает `expectedFrom`; несовпадение `data.from` → `bad_sender` с деталью got/expected (строки 1328–1329). ✅
- Lead-token route: `findUserById(user.userId)` → `wallet.accountName`; без кошелька → `409 { code: 'wallet_required' }` (3682–3686); `expectedFrom: leadWallet` в verifier (3693). ✅
- Job-posting route: та же привязка для homeowner (3766–3770), `expectedFrom: jobWallet` (3777). ✅
- Persistence сохранён: три таблицы с `tx_hash TEXT UNIQUE NOT NULL` + unique-индексы (474–504), `withPostgresTransaction` (270), `23505` → 409 replay (1389, 1451), нормализация hash в lowercase. ✅
- Итог: P1-1 восстановлен поверх P1-3, регресс устранён.

### Самостоятельно выполненные проверки (reviewer)
| Check | Command | Result |
|---|---|---|
| Static server check | `node --check pure-server.js` | PASS |
| Static test checks | `node --check tests/payments-402.test.js`, `node --check tests/payment-receipts-postgres.test.js` | PASS |
| Payment suite | `npx jest tests/payments-402.test.js --runInBand --detectOpenHandles` | PASS 18/18 (вкл. `wallet_required` для обоих ролей и unit `bad_sender`) |
| PostgreSQL integration | `TEST_DATABASE_URL=postgresql://...@localhost:55432/p13test npm run test:payment-receipts-postgres` | PASS 3/3 против изолированного Docker `postgres:16-alpine` (порт 55432); Railway/production НЕ использовался |

Note (LOW, не блокирует): при холодном первом запуске Postgres-suite `beforeAll` может не уложиться в дефолтный 5s jest hook timeout (первичное создание схемы на слабой машине); повторный запуск / `--testTimeout=60000` проходит стабильно. Рекомендация автору: добавить явный timeout в `beforeAll`.

### Sign-off (re-review)
- Reviewer decision: APPROVED
- Required checks: PASS
- Unresolved P0/P1 findings: 0
- Live-risk decision: BLOCKED (merge/deploy только после founder approval)
- Deploy decision: BLOCKED

## Test-Only Follow-up (CODEX, 2026-07-03)

- New head: `a81df24b5173e22fe5cd15c75bdb8651f6ee251a`.
- Changed only `v3/tests/payment-receipts-postgres.test.js`: `beforeAll(..., 30000)`.
- `node --check tests/payment-receipts-postgres.test.js`: PASS.
- `npx jest tests/payments-402.test.js --runInBand --detectOpenHandles`: PASS, 18/18.
- Isolated embedded PostgreSQL suite: PASS, 3/3; Railway/production was not used.
- Claude approval remains valid through `fd1284b`; the new test-only head is `NOT_REVIEWED` until a narrow Claude confirmation.

## Test-Only Re-Confirmation (CLAUDE, 2026-07-04, head `a81df24b`)

Узкое подтверждение test-only follow-up поверх ранее одобренного `fd1284b`.

- `git diff --stat fd1284b a81df24b` → изменён ровно один файл: `v3/tests/payment-receipts-postgres.test.js`, 1 insertion / 1 deletion.
- Содержимое diff: `beforeAll(async () => { ... })` → `beforeAll(async () => { ... }, 30000)` — только явный jest-timeout для холодного создания схемы. Это в точности рекомендация из моего re-review (`fd1284b`, LOW-заметка). Продакшн-код `pure-server.js` не изменён.
- Проверки на новом head:

| Check | Command | Result |
|---|---|---|
| Static server check | `node --check pure-server.js` | PASS (exit 0) |
| Static test checks | `node --check tests/payments-402.test.js`, `node --check tests/payment-receipts-postgres.test.js` | PASS (exit 0) |
| Payment suite | `npx jest tests/payments-402.test.js --runInBand --detectOpenHandles` | PASS 18/18 |

Изменение test-only и строго повышает устойчивость Postgres-suite (ранее PASS 3/3 на `fd1284b`); функциональное покрытие sender-binding + persistence не затронуто. Одобрение с `fd1284b` переносится на `a81df24b`.

## Current Sign-off

- Reviewer decision: APPROVED (head `a81df24b`)
- Required checks: PASS
- Unresolved P0/P1 findings: 0
- Live-risk decision: BLOCKED (merge/deploy только после founder approval)
- Founder evidence: PENDING
- Deploy decision: BLOCKED
